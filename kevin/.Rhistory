setwd("C:/Users/fuhai/Desktop/NYCDSA_Boot_Camp/Theory_review/Machine_Learning_Lab")
read.csv(file="Orders")
Orders <- read.csv("Orders.csv")
### Problem 1: Dataset Import & Cleaning
Check **"Profit"** and **"Sales"** in the dataset, convert these two columns to numeric type.
Orders <- read.csv("Orders.csv")
orders
Orders
Orders <- read.csv("Orders.csv")
Orders <- (Orders, Profit=as.numeric(Profit),
Sales, Sales=as.nemric(Sales))
Orders <- read.csv("Orders.csv")
Orders <- (Orders, Profit=as.numeric(Profit),
Sales=as.nemric(Sales))
Orders <- read.csv("Orders.csv")
Orders <- transform(Orders, Profit=as.numeric(Profit),
Sales=as.nemric(Sales))
Orders <- read.csv("Orders.csv")
Orders <- transform(Orders, Profit=as.numeric(Profit),
Sales=as.numeric(Sales))
Orders <- read.csv("Orders.csv")
Orders <- transform(Orders, Profit=as.numeric(Profit),
Sales=as.numeric(Sales))
head(Orders)
